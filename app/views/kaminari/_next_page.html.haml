%li.next
  - if current_page.last?
    %span.disabled
      = precede 'Next&nbsp;'.html_safe do
        %sapn.glyphicon.glyphicon-arrow-right
  - else
    = link_to raw(t 'views.pagination.next'), url, :remote => remote
