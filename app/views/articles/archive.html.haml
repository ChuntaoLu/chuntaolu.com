- title "Blog Archive"
%h1 Archive
- @articles_year.each do |date, articles|
  %h2= date.strftime('%Y')
  - articles.each do |article|
    .archive_article
      = date_string(article.created_at) + ' '
      = link_to article.title, article_path(article)
%br/
= link_to 'Back', articles_path
