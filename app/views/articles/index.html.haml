- title "Blog"
- if @tag
  %span.tag-intro= "#{@articles.count} posts tagged with"
  %span.tag= link_to @tag, articles_tag_path(@tag)
- @articles.each do |article|
  = render partial: 'articles/article', locals: { article: article }
= paginate @articles
.text-center
  = link_to(archives_articles_path, class: 'archives') do
    %i.fa.fa-archive.fa-2x{'data-toggle' => 'tooltip', 'title' => 'ARCHIVES'}